# 项目目录索引

## 项目概述

AI面试系统 - 基于Django框架构建的智能AI面试平台，支持语音面试、简历解析、实时对话等功能。

**项目根目录**: `E:\jiucai\projct\AIinterview-main\AIinterview-main\AIinterview-main`

---

## 快速导航

### 核心文件

| 文件/目录 | 说明 |
|-----------|------|
| `config/settings.py` | Django配置（数据库、模型、API密钥等） |
| `config/urls.py` | 全局URL路由 |
| `ai_interview/models.py` | 核心数据库模型 |
| `ai_interview/views/` | 拆分后的HTTP API视图 |
| `ai_interview/consumers.py` | WebSocket实时通信入口 |
| `ai_interview/services/` | 业务服务层 |
| `ai_interview/templates/` | HTML前端页面 |

### 面试类型

- **初次面试** (`initial_interview`)：六轮结构化面试，包含硬性字段、项目验证、技能评估等
- **面谈** (`discussion`)：开放对话式面试，由Omni模型主导，以招聘要求为最高优先级

---

## 目录结构

### 根目录

| 文件 | 说明 |
|------|------|
| `.env` | 本地环境变量（含密钥，请勿外传） |
| `.env.example` | 环境变量示例 |
| `README.md` | 项目说明文档 |
| `requirements.txt` | Python依赖列表 |
| `manage.py` | Django管理入口 |
| `run_server.bat` | 启动服务脚本 |
| `start_server.bat` | 启动服务脚本 |
| `stop_server.bat` | 停止服务脚本 |
| `generate_https_cert.bat` | 生成HTTPS证书脚本 |

### Django配置 (`config/`)

```
config/
├── __init__.py
├── settings.py        # Django配置（模型、API密钥、缓存等）
├── urls.py            # 全局URL路由
├── asgi.py            # ASGI入口（WebSocket支持）
└── wsgi.py            # WSGI入口
```

### 核心应用 (`ai_interview/`)

```
ai_interview/
├── __init__.py
├── apps.py            # Django app配置
├── models.py          # 核心模型（Candidate、JobConfiguration、TalentProfile等）
├── urls.py            # 应用URL路由
├── routing.py         # WebSocket路由
├── consumers.py       # 实时语音面试WebSocket消费者
├── views.py           # 旧/兼容视图入口
└── templates/         # HTML页面
```

### 视图模块 (`ai_interview/views/`)

| 文件 | 功能 |
|------|------|
| `candidate.py` | 候选人入口、身份校验、开始面试API |
| `hr.py` | HR管理API（候选人列表、删除、更新） |
| `interview.py` | 面试会话HTTP API |
| `report.py` | 问答报告数据和分析API |
| `resume.py` | 简历解析/校验API |
| `external.py` | 外部合作方接口 |
| `email.py` | 邮件发送API |
| `voice.py` | 语音TTS/ASR API |
| `utils.py` | 视图层工具函数 |

### 服务层 (`ai_interview/services/`)

| 文件 | 功能 |
|------|------|
| `session_manager.py` | 会话创建、恢复、保存、数据库同步 |
| `omni_stream_service.py` | Omni实时语音服务封装 |
| `qwen_service.py` | 通义文本模型调用 |
| `prompts.py` | 文本模型Prompt模板 |
| `interview_prompt_builder.py` | 按面试类型构建Prompt |
| `interview_types.py` | 面试类型常量和配置 |
| `resume_parser.py` | 简历解析服务 |
| `resume_async.py` | 异步简历解析处理 |
| `dialogue_engine.py` | 对话引擎逻辑 |
| `embedding_service.py` | 向量/Embedding服务 |
| `speech_service.py` | 语音服务 |
| `token_tracker.py` | Token使用统计 |

### 前端页面 (`ai_interview/templates/`)

| 文件 | 功能 |
|------|------|
| `hr_dashboard.html` | HR创建候选人、上传简历、选择面试类型 |
| `candidate_management.html` | HR候选人列表、报告入口、删除编辑 |
| `candidate_entry.html` | 候选人入口/身份校验页面 |
| `candidate_welcome.html` | 候选人欢迎页 |
| `interview.html` | 候选人面试/面谈页面 |
| `candidate_interview_report.html` | 问答报告页面 |
| `send_email.html` | 发送邮件页面 |
| `token_dashboard.html` | Token使用看板 |

### 数据库迁移 (`ai_interview/migrations/`)

包含数据库结构变更记录，通过 `python manage.py migrate` 应用。

### 管理命令 (`ai_interview/management/commands/`)

- `clear_interview_data.py` - 清理面试数据命令

---

## 关键模型说明

### 核心数据库模型

| 模型 | 说明 |
|------|------|
| `Candidate` | 候选人信息 |
| `JobConfiguration` | 岗位配置（面试类型、招聘要求等） |
| `TalentProfile` | 候选人画像（问答记录、报告分析） |
| `ExternalAISession` | 外部系统会话记录 |
| `TokenUsage` | Token使用统计 |

### 面试流程

```
HR创建面试 → 生成面试链接 → 候选人访问 → 身份验证 → 开始面试 → 语音交互 → 面试结束 → 生成报告
```

---

## 常用命令

```powershell
# 启动开发服务器
python manage.py runserver

# 创建数据库迁移
python manage.py makemigrations

# 应用数据库迁移
python manage.py migrate

# 启动HTTPS开发服务器
python manage.py runsslserver 0.0.0.0:443 --certificate cert.pem --key key.pem
```

---

## 环境变量配置

| 变量 | 说明 |
|------|------|
| `DASHSCOPE_API_KEY` | 阿里云DashScope API密钥 |
| `QWEN_MODEL` | 文本模型（qwen3.5-omni-plus） |
| `QWEN_REALTIME_MODEL` | 实时语音模型 |
| `QWEN_EMBEDDING_MODEL` | 向量模型 |
| `DEBUG` | 调试模式 |
| `ALLOWED_HOSTS` | 允许的主机列表 |