# AI面试系统流程分析

## 概述

本系统采用"Omni模型主导 + Python透传"的极简架构，让大语言模型全权负责面试流程，Python代码只负责消息透传和状态保存。

---

## 架构设计

### 核心思想

```
┌─────────────────────────────────────────────────┐
│              Omni 模型 (核心智能)                │
│  - 负责面试流程管理                             │
│  - 判断回答质量                                 │
│  - 决定追问和轮次过渡                           │
└───────────────────────────┬───────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────┐
│              Python (透传层)                     │
│  - 透传音频/文本消息                            │
│  - 保存对话历史                                 │
│  - 管理会话状态                                 │
└─────────────────────────────────────────────────┘
```

---

## 完整调用流程

### 阶段1：WebSocket连接建立

| 步骤 | 说明 |
|------|------|
| 1 | 用户点击"开始面试"，前端建立WebSocket连接 |
| 2 | `consumers.py` 初始化会话状态 |
| 3 | 前端发送 `type: "start_interview"` 消息 |

### 阶段2：初始化Omni连接

| 步骤 | 说明 |
|------|------|
| 4 | 判断是新建会话还是恢复会话 |
| 5 | 创建Omni实例并配置参数 |
| 6 | 设置基础System Prompt |

### 阶段3：发送完整面试Prompt

这是核心步骤，系统构建完整的面试指令发送给Omni：

```
【开始面试】

你是一个专业、友好的AI面试官。现在开始进行一场结构化面试。

面试者信息：
目标岗位：XXX
职级要求：XXX
岗位描述：XXX

候选人简历摘要：XXX

面试规则：
1. 面试共6个维度（部分可跳过）
2. 高质量回答直接进入下一轮
3. 低质量回答需要追问（最多1次）
4. 所有轮次完成后说"面试结束了，感谢你的时间"

现在，请开始面试。直接问第一个问题！
```

### 阶段4：Omni生成回复

| 步骤 | 说明 |
|------|------|
| 10 | Omni流式生成文本和音频 |
| 11 | 音频流实时发送给前端 |
| 12 | 文本流实时发送给前端 |
| 13 | 完整文本生成完成 |

### 阶段5：处理AI回复

| 步骤 | 说明 |
|------|------|
| 14 | 保存到对话历史 |
| 15 | 检查是否面试结束（关键词匹配） |
| 16 | 推断当前轮次（用于UI显示） |
| 17 | 发送状态给前端 |

### 阶段6：用户回答（透传）

| 步骤 | 说明 |
|------|------|
| 18 | 用户说话，音频流持续发送 |
| 19 | Python直接透传给Omni |
| 20 | Omni自己判断：是否说完、回答质量、是否追问 |
| 21 | 重复阶段4-5，继续面试 |

---

## 面试类型

### 初次面试（六轮结构化）

| 轮次 | 名称 | 说明 |
|------|------|------|
| 1 | 硬性指标补全 | 验证基本条件（可跳过） |
| 2 | 项目角色真实性 | 验证项目经历 |
| 3 | 项目角色深度 | 考察技术深度 |
| 4 | 技能偏差验证 | 验证技能匹配度 |
| 5 | 语言逻辑能力 | 评估表达能力 |
| 6 | 自定义问题 | HR自定义问题（可跳过） |

### 面谈模式（开放对话）

- 以HR输入的招聘要求为最高优先级
- 职级和简历作为参考
- Omni完全主导对话流程
- 结束时说"面谈结束"

---

## 状态保存机制

### 存储位置

| 位置 | 类型 | 说明 |
|------|------|------|
| 内存缓存 | 临时存储 | 会话期间实时数据 |
| SQLite数据库 | 持久存储 | `TalentProfile`模型 |

### 存储时机

| 时机 | 说明 |
|------|------|
| 每次AI回复后 | 追加到对话历史 |
| WebSocket断开 | 保存最终状态 |
| 前端主动保存 | 手动触发保存 |

---

## 断点续面流程

1. 检测到存在对话历史
2. 构建"断点续面"Prompt（包含最近对话）
3. Omni根据历史继续面试

---

## 打断机制

1. 用户点击打断或开始说话
2. 设置中断标志，停止delta发送
3. 调用Omni取消响应
4. 清空音频缓冲

---

## 核心优势

1. **代码极简**：职责分离，Python代码量大幅减少
2. **逻辑清晰**：Omni负责智能决策，Python负责基础操作
3. **易于维护**：所有规则集中在Prompt中
4. **更智能**：充分发挥大模型能力

---

## 配置参数

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `QWEN_REALTIME_VAD_THRESHOLD` | VAD阈值 | 0.5 |
| `QWEN_REALTIME_VAD_SILENCE_MS` | 静默等待时间 | 1500ms |
| `QWEN_REALTIME_VOICE` | 语音音色 | Sunnybobi |